import type { Metadata } from "next";
import { Inter, Playfair_Display } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"], variable: "--font-inter" });
const playfair = Playfair_Display({ subsets: ["latin"], variable: "--font-playfair" });

export const metadata: Metadata = {
  title: "RWA Platform — Real World Asset Tokenization",
  description:
    "Invest in tokenized real estate, bonds, infrastructure, fine art and commodities. Fractional ownership starting from $100.",
  keywords: ["RWA", "tokenization", "real estate", "blockchain", "DeFi", "investment"],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={`${inter.variable} ${playfair.variable}`}>
      <body>{children}</body>
    </html>
  );
}
